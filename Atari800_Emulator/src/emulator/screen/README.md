# TV Screen Emulator

The Atari 800 needs to output to a screen; however, in order for this to work we need to emulate the screen using NCurses or a wrapper around NCurses that allows us to actually put pixels on the screen, using commands that the Atari 800 would be outputing via its graphics ICs.


## NCurses || ftxui

