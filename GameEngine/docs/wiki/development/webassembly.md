# WebAssembly 

The overall goal of this repo is to build a game engine that can be cross-compiled into `WebAssembly` using WebGL for browser based applications as well as directly to OpenGL & Vulkan frameworks for native applications. At this point in development this is purely theoretical; however, the guiding principle is to abstract this way from the user. 